{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the overlay window",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default",
    "updater:default",
    "process:default"
  ]
}
